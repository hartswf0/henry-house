node_modules/
.DS_Store
*.log

# working frames from tools/render/shoot-one.mjs — low sample counts, rendered
# to answer "did that change do what I think", never the published set
out/preview/

# generated builder traces — the working directory. The COPIES under
# assets/traces/ are committed on purpose: trace.html serves them.
out/traces/
assets-traces-dropin.zip
henry-house-traces.zip
