node_modules/
.DS_Store
*.log

# working frames from tools/render/shoot-one.mjs — low sample counts, rendered
# to answer "did that change do what I think", never the published set
out/preview/

# generated build chains — regenerate with tools/export-chains.mjs.
# The zip itself IS committed: it is the deliverable, and a 5 MB bag of JSON that
# somebody has to be able to download is worth more in the repository than the
# instruction to rebuild it.
out/henry-house-chains/
out/traces/
henry-house-traces.zip
